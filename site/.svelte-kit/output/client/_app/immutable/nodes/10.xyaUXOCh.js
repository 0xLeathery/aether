import{a as t,f as r}from"../chunks/DFIm6B6S.js";import"../chunks/D677U3-6.js";import{t as s,a8 as a,$ as c,a9 as l}from"../chunks/0fw90vus.js";import{h}from"../chunks/DB5xlG3i.js";import{s as p}from"../chunks/DAmNXEaF.js";const d={title:"Networking",description:"How Aether peers discover each other, establish connections, and communicate using libp2p."},{title:m,description:u}=d;var f=r('<meta name="description"/>'),g=r('<h1 id="networking-protocol"><a href="#networking-protocol">Networking Protocol</a></h1> <blockquote><p>This is a simplified overview of Aether’s networking layer, not an exhaustive protocol specification.</p></blockquote> <p>Aether’s networking is built on <strong><code>libp2p</code> 0.56</strong> (Rust implementation), a modular peer-to-peer networking framework originally created for IPFS. <code>libp2p</code> handles peer discovery, connection management, and protocol multiplexing — letting Aether focus on application logic rather than socket programming.</p> <h2 id="transport"><a href="#transport">Transport</a></h2> <p>Aether uses <strong>TCP</strong> as its transport protocol. This is a deliberate choice: Aether’s swarm isolation relies on Pre-Shared Key (PSK) encryption using XSalsa20, which operates at the transport layer. QUIC — the more modern alternative — has its own TLS layer built in, and that TLS layer conflicts with PSK encryption. TCP avoids this conflict.</p> <p>All connections are direct peer-to-peer. There are no relay servers in the current architecture.</p> <h2 id="peer-discovery"><a href="#peer-discovery">Peer Discovery</a></h2> <p>Finding other peers happens through two mechanisms:</p> <p><strong>Kademlia DHT (Distributed Hash Table)</strong> — For internet-wide discovery. Peers announce themselves to the DHT and query it to find others who share the same swarm key. The DHT is distributed across all participating peers, so there is no central directory.</p> <p><strong>mDNS (Multicast DNS)</strong> — For local network discovery. If two Aether peers are on the same LAN, they can find each other without any internet connectivity. This is especially useful for offline or air-gapped environments.</p> <h2 id="nat-traversal"><a href="#nat-traversal">NAT Traversal</a></h2> <p>Most home networks sit behind a NAT (Network Address Translation) router, which makes direct incoming connections difficult. Aether uses STUN-like techniques for UDP holepunching to establish direct connections through NAT.</p> <p>This works for most NAT types (full cone, restricted cone, port-restricted cone). However, <strong>symmetric NAT</strong> — found in approximately 10-20% of consumer networks — prevents holepunching. A TURN relay would solve this but is not currently implemented, as it requires server infrastructure (which has a cost). This is a known limitation.</p> <h2 id="psk-swarm-isolation"><a href="#psk-swarm-isolation">PSK Swarm Isolation</a></h2> <p>Each Aether swarm is protected by a <strong>Pre-Shared Key</strong> — a 256-bit secret that acts as a network-level access control. The PSK enables XSalsa20 stream encryption via <code>libp2p</code>’s <code>PnetConfig</code> (Private Network Configuration).</p> <p>This means:</p> <ul><li>Only peers who possess the correct PSK can participate in the swarm</li> <li>Encryption happens at the transport layer, before any application data is exchanged</li> <li>A peer without the key cannot even establish a connection — the handshake fails immediately</li></ul> <p>Think of it as a locked room: you need the key to enter. Everyone inside can communicate freely. See the <a href="/docs/architecture/encryption">Encryption</a> page for the full security model and its limitations.</p> <h2 id="connection-lifecycle"><a href="#connection-lifecycle">Connection Lifecycle</a></h2> <ol><li><strong>Bootstrap</strong> — Peer starts and connects to known DHT bootstrap nodes (or discovers local peers via mDNS)</li> <li><strong>Announce</strong> — Peer registers itself on the DHT with its swarm identifier</li> <li><strong>Discover</strong> — Peer queries the DHT for other peers in the same swarm</li> <li><strong>Connect</strong> — Direct TCP connection established with PSK handshake</li> <li><strong>Multiplex</strong> — Multiple protocols (chat sync, voice, control messages) run over a single connection</li></ol> <h2 id="related-pages"><a href="#related-pages">Related Pages</a></h2> <ul><li><a href="/docs/architecture">Architecture Overview</a> — High-level system diagram</li> <li><a href="/docs/architecture/encryption">Encryption</a> — PSK security model and limitations</li> <li><a href="/docs/architecture/voice">Voice</a> — Audio streaming over <code>libp2p</code></li> <li><a href="/docs/architecture/crdts">CRDTs</a> — Data sync over established connections</li></ul>',1);function b(o){var i=g();h("19316mt",n=>{var e=f();s(()=>p(e,"content",u)),a(()=>{c.title=`${m} - Aether Docs`}),t(n,e)}),l(42),t(o,i)}export{b as component};

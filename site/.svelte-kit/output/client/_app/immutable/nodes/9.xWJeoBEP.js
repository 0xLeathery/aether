import{a as t,f as i}from"../chunks/DFIm6B6S.js";import"../chunks/D677U3-6.js";import{t as n,a8 as o,$ as d,a9 as l}from"../chunks/0fw90vus.js";import{h as c}from"../chunks/DB5xlG3i.js";import{s as h}from"../chunks/DAmNXEaF.js";const p={title:"Identity",description:"How Aether creates and manages cryptographic identities using Ed25519 keys and the OS system keychain."},{title:y,description:u}=p;var g=i('<meta name="description"/>'),m=i('<h1 id="identity-system"><a href="#identity-system">Identity System</a></h1> <blockquote><p>This is a simplified overview of Aether’s identity model, not a cryptographic specification.</p></blockquote> <p>Every Aether peer has a unique cryptographic identity. There is no sign-up flow, no email address, and no username registry. Your identity is a keypair generated on your device and stored in your operating system’s secure keychain.</p> <h2 id="ed25519-keypair"><a href="#ed25519-keypair">Ed25519 Keypair</a></h2> <p>Aether uses <strong>Ed25519</strong> (elliptic curve cryptography) for identity. When you first launch Aether, the app generates a public/private keypair:</p> <ul><li><strong>Public key</strong> — Your globally unique identifier. Shared with every peer you connect to. The 256-bit key space makes collisions effectively impossible.</li> <li><strong>Private key</strong> — Proves you are who you claim to be. Used to sign messages. Never transmitted, never exported.</li></ul> <p>Ed25519 was chosen for its speed (thousands of signatures per second), small key size (32 bytes), and resistance to timing attacks.</p> <h2 id="keychain-storage"><a href="#keychain-storage">Keychain Storage</a></h2> <p>The private key is stored in the <strong>OS system keychain</strong>:</p> <ul><li><strong>macOS</strong> — Keychain Services (with ACLs configured to avoid password prompts in production builds)</li> <li><strong>Windows</strong> — Credential Manager</li> <li><strong>Linux</strong> — Secret Service API (GNOME Keyring, KDE Wallet, etc.)</li></ul> <p>The keychain provides hardware-backed or OS-level protection depending on the platform. On Macs with a Secure Enclave, the key benefits from hardware isolation. On all platforms, the key is protected by the user’s OS login credentials.</p> <h2 id="hardware-binding"><a href="#hardware-binding">Hardware Binding</a></h2> <p>Aether’s identity is <strong>bound to the device</strong>. This is a deliberate design choice:</p> <ul><li>No seed phrases</li> <li>No cloud backup</li> <li>No export functionality</li> <li>No multi-device identity sync</li></ul> <p>If you lose the device, you lose the identity. This is the trade-off: <strong>security over convenience</strong>. A key that cannot be exported cannot be stolen remotely. There is no “forgot password” flow because there is no password — only the key on your hardware.</p> <p>For users who need presence on multiple devices, each device has its own independent identity.</p> <h2 id="display-names"><a href="#display-names">Display Names</a></h2> <p>Names in Aether are <strong>self-asserted</strong>. You can claim any display name you want. There is no uniqueness enforcement and no central registry to check against.</p> <p>This means two peers could both call themselves “Alice.” Rather than solving this with a central authority, Aether uses <strong>petnames</strong>: any peer can assign a local alias to any other peer. If your friend calls themselves “xX_DarkLord_Xx” but you know them as “Dave,” you can override their name locally. Only you see the petname — it is not transmitted.</p> <p>This is the sovereign naming model: no central authority decides what you are called. Your self-asserted name is a suggestion; each peer decides what to display.</p> <h2 id="verification"><a href="#verification">Verification</a></h2> <p>Peers are ultimately identified by their <strong>public key</strong>, not their display name. When no display name is set, Aether falls back to a truncated version of the public key as an identifier. This ensures every peer always has a unique, unforgeable label — even if display names collide.</p> <h2 id="related-pages"><a href="#related-pages">Related Pages</a></h2> <ul><li><a href="/docs/architecture">Architecture Overview</a> — Where identity fits in the system stack</li> <li><a href="/docs/architecture/encryption">Encryption</a> — How keys are used for authentication and transport security</li> <li><a href="/docs/architecture/networking">Networking</a> — How peers discover and connect to each other</li></ul>',1);function A(a){var r=m();c("rcegfx",s=>{var e=g();n(()=>h(e,"content",u)),o(()=>{d.title=`${y} - Aether Docs`}),t(s,e)}),l(46),t(a,r)}export{A as component};

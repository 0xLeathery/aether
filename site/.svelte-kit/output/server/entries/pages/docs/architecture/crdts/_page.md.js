import { a3 as head, e as escape_html, a as attr } from "../../../../../chunks/index.js";
const metadata = {
  "title": "CRDTs",
  "description": "How Aether uses Automerge CRDTs for conflict-free data synchronization between peers."
};
const { title, description } = metadata;
function _page_md($$renderer) {
  head("1pxm1ij", $$renderer, ($$renderer2) => {
    $$renderer2.title(($$renderer3) => {
      $$renderer3.push(`<title>${escape_html(title)} - Aether Docs</title>`);
    });
    $$renderer2.push(`<meta name="description"${attr("content", description)}/>`);
  });
  $$renderer.push(`<h1 id="crdts-conflict-free-replicated-data-types"><a href="#crdts-conflict-free-replicated-data-types">CRDTs (Conflict-Free Replicated Data Types)</a></h1> <blockquote><p>This is a simplified overview of Aether’s data synchronization model, not a formal CRDT specification.</p></blockquote> <p>In a peer-to-peer system, there is no central server to decide the “correct” state of the data. Two peers might make changes at the same time while disconnected. When they reconnect, those changes need to merge without conflicts. This is the problem CRDTs solve.</p> <h2 id="what-are-crdts"><a href="#what-are-crdts">What Are CRDTs</a></h2> <p>A <strong>Conflict-Free Replicated Data Type</strong> is a data structure that can be modified independently by multiple peers and merged automatically — without coordination and without conflicts. Changes always converge to the same state regardless of the order they are applied.</p> <p>Traditional databases use locking or consensus protocols to prevent conflicts. CRDTs take the opposite approach: they are mathematically designed so that conflicts are impossible. Every valid merge produces the same result.</p> <h2 id="automerge"><a href="#automerge">Automerge</a></h2> <p>Aether uses the <strong>Automerge</strong> library (Rust crate with <code>derive</code> macros) for all CRDT operations. Automerge provides a document model similar to JSON, but with built-in merge semantics.</p> <p>Each piece of shared state in Aether — chat history, channel metadata — is an Automerge document. These documents are the single source of truth.</p> <h2 id="how-sync-works"><a href="#how-sync-works">How Sync Works</a></h2> <ol><li><strong>Local-first</strong> — Changes are applied to your local copy of the document immediately. There is no “saving to server” step.</li> <li><strong>Peer connection</strong> — When two peers connect (via <a href="/docs/architecture/networking">libp2p</a>), they exchange Automerge sync messages.</li> <li><strong>Merge</strong> — Each peer integrates the other’s changes into their local document. Automerge’s merge algorithm ensures both arrive at the same state.</li> <li><strong>Eventual consistency</strong> — All peers converge to the same state, but there may be a delay while they are disconnected. This is the “eventual” part: consistency is guaranteed, but not instantaneous.</li></ol> <p>Data moves only when peers are online and connected. There is no background sync service and no push notifications. If you close Aether, your data stays frozen until you open it again and reconnect with peers.</p> <h2 id="chat-messages"><a href="#chat-messages">Chat Messages</a></h2> <p>Each chat message is an entry in an Automerge list. A message contains:</p> <ul><li><strong>Sender</strong> — The public key of the peer who wrote it (see <a href="/docs/architecture/identity">Identity</a>)</li> <li><strong>Timestamp</strong> — When the message was created (local clock)</li> <li><strong>Content</strong> — The message text</li></ul> <p>New messages are appended to the list. When two peers send messages at the same time, Automerge’s list merge semantics place them in a deterministic order. There is no server-side ordering — the CRDT handles it.</p> <h2 id="channel-metadata"><a href="#channel-metadata">Channel Metadata</a></h2> <p>Channel names, creation timestamps, and archive status are stored in an Automerge map. Changes to channels (rename, archive) sync through the same mechanism as chat messages.</p> <h2 id="trade-offs"><a href="#trade-offs">Trade-offs</a></h2> <p>CRDTs are not free. There are costs to be aware of:</p> <p><strong>History growth</strong> — Automerge tracks the full edit history of a document to enable merging. This means CRDT documents grow over time. Aether does not compact or garbage-collect old state in v1. For small-to-medium groups (the intended use case), this is manageable. Very large histories with thousands of messages may eventually impact performance.</p> <p><strong>No deletion guarantees</strong> — In a peer-to-peer system, you cannot force all peers to delete data. A “delete” operation is really a “mark as deleted” — the tombstone must be retained so other peers know to apply the deletion when they sync.</p> <p><strong>Clock skew</strong> — Message timestamps use local device clocks. If a peer’s clock is wrong, their messages may appear out of order. Automerge’s causal ordering mitigates this for concurrent edits, but display order still relies on timestamps.</p> <h2 id="related-pages"><a href="#related-pages">Related Pages</a></h2> <ul><li><a href="/docs/architecture">Architecture Overview</a> — Where CRDTs fit in the system stack</li> <li><a href="/docs/architecture/networking">Networking</a> — How sync messages are transported between peers</li> <li><a href="/docs/architecture/encryption">Encryption</a> — How CRDT data is protected in transit (and not at rest)</li></ul>`);
}
export {
  _page_md as default,
  metadata
};

import { a3 as head, e as escape_html, a as attr } from "../../../../../chunks/index.js";
const metadata = {
  "title": "Encryption",
  "description": "What Aether encrypts, what it does not, and why -- an honest accounting of the security model."
};
const { title, description } = metadata;
function _page_md($$renderer) {
  head("4fh9vk", $$renderer, ($$renderer2) => {
    $$renderer2.title(($$renderer3) => {
      $$renderer3.push(`<title>${escape_html(title)} - Aether Docs</title>`);
    });
    $$renderer2.push(`<meta name="description"${attr("content", description)}/>`);
  });
  $$renderer.push(`<h1 id="encryption"><a href="#encryption">Encryption</a></h1> <blockquote><p>This page documents Aether’s encryption scope honestly, including its limitations. Aether provides transport-only encryption — not end-to-end encryption in the Signal or WhatsApp sense.</p></blockquote> <h2 id="what-is-encrypted"><a href="#what-is-encrypted">What IS Encrypted</a></h2> <h3 id="network-transport"><a href="#network-transport">Network Transport</a></h3> <p>All peer-to-peer traffic is encrypted at the <code>libp2p</code> transport layer using <strong>PSK (Pre-Shared Key) encryption with XSalsa20</strong>. An eavesdropper monitoring your network traffic cannot read your messages, hear your voice calls, or see your channel metadata.</p> <p>This encryption is always on. There is no unencrypted mode.</p> <h3 id="swarm-isolation"><a href="#swarm-isolation">Swarm Isolation</a></h3> <p>Each swarm’s PSK acts as both an access control mechanism and an encryption key. Only peers who possess the correct 256-bit key can participate in the swarm. Non-members cannot even establish a connection — the PSK handshake fails at the transport layer before any application data is exchanged.</p> <h3 id="authentication"><a href="#authentication">Authentication</a></h3> <p>Ed25519 key signing ensures that messages come from who they claim to be from. Each peer’s <a href="/docs/architecture/identity">identity</a> is verified cryptographically. Spoofing another peer’s identity would require possessing their private key, which never leaves their device.</p> <h2 id="what-is-not-encrypted"><a href="#what-is-not-encrypted">What is NOT Encrypted</a></h2> <h3 id="local-storage-is-not-encrypted"><a href="#local-storage-is-not-encrypted">Local Storage is Not Encrypted</a></h3> <p>Chat messages, channel metadata, and <a href="/docs/architecture/crdts">CRDT</a> state are stored in <strong>cleartext on your local filesystem</strong>. Anyone with access to your device can read your messages.</p> <p>This is a deliberate v1 design choice. Cleartext local storage enables:</p> <ul><li>Full-text search across message history</li> <li>Easier debugging during development</li> <li>Simpler implementation with fewer failure modes</li></ul> <p>The trade-off is explicit: if someone gains access to your machine (physically or via malware), they can read your Aether data. Your OS login credentials are your primary defense for local data.</p> <h3 id="no-end-to-end-encryption-for-message-content"><a href="#no-end-to-end-encryption-for-message-content">No End-to-End Encryption for Message Content</a></h3> <p>The PSK encrypts the transport, but <strong>all swarm members share the same key</strong>. There is no per-message encryption between specific peers. Every member of a swarm can read every message in that swarm.</p> <p>Think of it as a room with a locked door: you need the key to enter, but once inside, everyone can hear everything. This is the intended model for group communication — a shared space, not a set of private channels.</p> <h3 id="no-forward-secrecy"><a href="#no-forward-secrecy">No Forward Secrecy</a></h3> <p>If a swarm’s PSK is compromised, past network traffic that was captured by an eavesdropper could theoretically be decrypted. The key does not rotate automatically.</p> <p><strong>Mitigation:</strong> Create a new swarm with a new key. The old swarm’s key remains compromised, but the new swarm starts with a fresh secret.</p> <h3 id="no-at-rest-encryption"><a href="#no-at-rest-encryption">No At-Rest Encryption</a></h3> <p>The OS system keychain protects your Ed25519 private key, but your <strong>message database is not encrypted on disk</strong>. This is distinct from the “local storage” point above — it applies to all persisted application data, including CRDT documents and any cached state.</p> <h2 id="security-model-summary"><a href="#security-model-summary">Security Model Summary</a></h2> <p>Aether uses a <strong>“locked room” model</strong>:</p> <table><thead><tr><th>Layer</th><th>Protection</th><th>Status</th></tr></thead><tbody><tr><td>Network transport</td><td>XSalsa20 via PSK</td><td>Encrypted</td></tr><tr><td>Swarm membership</td><td>PSK handshake</td><td>Protected</td></tr><tr><td>Peer authentication</td><td>Ed25519 signing</td><td>Verified</td></tr><tr><td>Local message storage</td><td>None</td><td>Cleartext</td></tr><tr><td>Database on disk</td><td>None</td><td>Cleartext</td></tr><tr><td>Per-message encryption</td><td>None</td><td>Not implemented</td></tr><tr><td>Forward secrecy</td><td>None</td><td>Not implemented</td></tr></tbody></table> <p>This is <strong>transport-only encryption</strong>. Data is protected while it moves between peers. Data is not protected where it is stored.</p> <p>For Aether’s intended use case — small trusted groups who want to communicate without depending on a server — this model is appropriate. The PSK ensures outsiders cannot access the swarm. Within the swarm, trust is assumed.</p> <p>For communications requiring forward secrecy, at-rest encryption, or per-message confidentiality, additional cryptographic layers would be needed.</p> <h2 id="future-improvements"><a href="#future-improvements">Future Improvements</a></h2> <p>These are planned but <strong>not yet implemented</strong>:</p> <ul><li><strong>At-rest database encryption</strong> — Encrypt the local message store, requiring a user-provided passphrase to unlock</li> <li><strong>Per-peer encrypted channels</strong> — Double ratchet or similar protocol for private messages between specific peers within a swarm</li> <li><strong>Forward secrecy via key rotation</strong> — Automatic PSK rotation to limit the window of compromise</li></ul> <h2 id="related-pages"><a href="#related-pages">Related Pages</a></h2> <ul><li><a href="/docs/architecture">Architecture Overview</a> — Where encryption fits in the system stack</li> <li><a href="/docs/architecture/networking">Networking</a> — Transport layer and PSK swarm isolation</li> <li><a href="/docs/architecture/identity">Identity</a> — Ed25519 keys and keychain storage</li></ul>`);
}
export {
  _page_md as default,
  metadata
};
